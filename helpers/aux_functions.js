// Prepare standard answer object
export function prepareAnswer(responseCode, responseResult, responseType){
  return {responseCode: responseCode, responseResult: responseResult, responseType: responseType};
}